<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/search_product_bar"
    android:orientation="horizontal"
    android:maxHeight="48dp"
    android:paddingHorizontal="25dp"
    android:paddingVertical="3dp">

    <EditText
        android:id="@+id/search_bar_edittext"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:ems="10"
        android:hint="Поиск продуктов"
        android:textColorHint="@color/grey"
        android:inputType="textCapWords|textAutoCorrect"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/search_bar_barcode"
        app:layout_constraintStart_toEndOf="@+id/search_bar_hint_icon"
        app:layout_constraintTop_toTopOf="parent"
        android:textColor="@color/grey_dark"
        android:background="@null"
        android:maxLines="1"
        android:lines="1" />

    <ImageButton
        android:id="@+id/search_bar_barcode"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/search_bar_edittext"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@android:drawable/ic_menu_camera"
        android:onClick="goToBarcodeScanActivity"/>

    <ImageView
        android:id="@+id/search_bar_hint_icon"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/search_bar_edittext"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@android:drawable/ic_menu_search" />

</androidx.constraintlayout.widget.ConstraintLayout>